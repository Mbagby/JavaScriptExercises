merge(left,right){
	var leftI = 0;
	var rightI =0;
	var result = [];
	loop (leftI < left.length && rightI < right.length)
		if left[leftI] >= right[rightI]
			result.push(left[leftI])
		if right[i] > left[i]
			result.push(right[rightI])


}