module.exports = {
	parenthesesChecker: function(string){
		var baseString= "()"
		var baseString[0]= 1
		var baseString[1]= 2
		if (array[0]=== 2 || array[(array.length-1)]=== 1 || array.length % 2 !=0){
			return "False"
			}
		var value = 0;
		else if for (i= 0; i < array.length; i ++){
			(value += array[i]);
				if (value % 2 === 0){
					return "False"
				}else{
					return "True"
				}
			}
		}
}

